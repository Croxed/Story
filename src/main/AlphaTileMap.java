package main;

public class AlphaTileMap {

	private int TILE_SIZE = 16;
	private int WIDTH = 1024;
	private int HEIGHT = 768;

	/*
	 * A lightning system based upon a custom TileMap implementation.
	 * 
	 * Each "Tile" has an alpha value assigned to it. Every tile is then
	 * merged together to form a single image which is then overlayed on the
	 * actual game in order to form an artificial light effect.
	 */
	public AlphaTileMap()
	{

	}
}